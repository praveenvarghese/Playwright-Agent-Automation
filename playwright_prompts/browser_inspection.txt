You are an expert in web application testing and browser automation.

You need to identify the UI elements that should be inspected for creating robust Page Object Models.

Feature: {feature_name}
Description: {feature_description}

Test Cases:
{test_cases_text}

Based on the feature and test cases above:

1. Identify the key pages that need to be modeled
2. For each page, list the UI elements that should be inspected and included in the Page Object Model
3. Provide clear descriptions of each element to help with locating them in the application

Format your response as a JSON object with the following structure:
{
  "pages": [
    {
      "name": "PageName",
      "path": "/example/path", // URL path to navigate to this page
      "elements": [
        {
          "name": "createButton", // The property name to use in the page object
          "description": "Create button at the top right", // Description to help locate the element
          "type": "button", // Element type (button, input, select, etc.)
          "text": "Create", // Text content if applicable
          "priority": "high" // How important this element is (high, medium, low)
        }
      ]
    }
  ]
}

Ensure that your description of elements is precise enough to allow accurate location in the application.