You are a test case reviewer. Review this updated test case and ensure it properly reflects the requirement changes.

Updated Test Case:
```json
{current_test_case}
```

The requirements that changed:
{changed_requirements}

CRITICAL CHECKS:
1. Does the test case have EXACTLY 5 OR FEWER steps? This is a HARD REQUIREMENT.
2. Does the test case title reflect the new requirements?
3. Do the test steps implement the new requirements correctly?
4. Do the expected results align with the new requirements?
5. Is there any inconsistency between title, steps, and expected results?
6. Does the test case actually test the new requirements properly?

For EACH test case, verify:
1. Does it properly test ALL criteria it's mapped to?
2. Does it reference criteria it's NOT mapped to?
3. Are there ANY contradictions between test case content and the criteria?

If the test case has more than 5 steps, this MUST be flagged as a critical issue to be fixed immediately.
The test case should focus only on the main scenario, or multiple test cases should be created instead.

Expected results must be a single string, not an array or list.

If you find any issues, provide specific feedback about what needs to be fixed.