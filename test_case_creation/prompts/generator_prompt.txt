For each identified requirement, generate test cases in JSON format. Each test case should include these exact fields:

{
  "id": "TC-ENV-[Number]",  
  "title": "Clear, action-oriented title",
  "steps": [
    "1. First step",
    "2. Second step",
    "3. Third step",
    "4. Fourth step",
    "5. Fifth step"
  ],
  "expectedResults": "Observable outcomes"
}

IMPORTANT GUIDELINES:
- Each test case MUST test only ONE specific functionality
- STRICTLY limit to 5 steps maximum per test case
- Break complex scenarios into multiple test cases
- Each test case should verify a specific acceptance criteria
- Keep expected results clear and verifiable for each specific test

Ensure you:
- Use only letters, numbers, underscore, dash in the ID
- Format the output as a JSON array containing all test cases

YOUR RESPONSE MUST BE A VALID JSON ARRAY ONLY. Do not include any text before or after the JSON array. 
The response should start with '[' and end with ']' with no additional text, comments, or explanations.